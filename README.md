# Dezhi Yu Resume

Personal resume and portfolio site built with HugoBlox and Hugo Extended.

## Development

```bash
pnpm install
pnpm run dev
```

The local site is served at `http://localhost:1313/`.

## Production Build

```bash
pnpm run build
```

The generated site is written to `public/`.
